In the main \target_water_table_depth there are plots of the series and splits,
dataset in .csv format and normalization factors. 

In \seq2val .npy object are stored which are the actual object inserted into the models